CREATE DATABASE database_application_ubs;

CREATE DATABASE spring_batch_audit;

CREATE TABLE product_item (
	id INT NOT NULL AUTO_INCREMENT,
	product VARCHAR(20) NOT NULL,
	quantity INT,
	price VARCHAR(20),
	type VARCHAR(10) NOT NULL,
	industry VARCHAR(120),
	origin VARCHAR(10),
	PRIMARY KEY (id)
);